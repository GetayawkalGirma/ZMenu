generator client {
  provider        = "prisma-client-js"
  engineType      = "library"
  previewFeatures = ["prismaSchemaFolder", "driverAdapters"]
}

datasource db {
  provider = "postgresql"
  schemas  = ["menu", "restaurant", "storage"]
}
