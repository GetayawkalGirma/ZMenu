generator client {
  provider        = "prisma-client-js"
  engineType      = "library"
  previewFeatures = ["prismaSchemaFolder"]
}

datasource db {
  provider = "postgresql"
  schemas  = ["menu", "restaurant", "storage"]
}
